{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "npx @biomejs/biome check --write \"$CLAUDE_FILE_PATH\" 2>/dev/null || true"
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "echo \"$CLAUDE_FILE_PATH\" | grep -qE '(dist/|package-lock\\.json)' && echo 'BLOCK: Edit dist/ via npm run build, not directly' && exit 1 || exit 0"
      }
    ]
  }
}
